# ⚠️ STATIC FALLBACK TEMPLATE for Codex review
# Used by `bash scripts/configure-actions.sh --static-templates` when Codex is the chosen provider.
# Default render path: scripts/render-workflows.sh writes a project-tailored version.
#
# Reference: https://github.com/openai/codex-action

name: Codex Review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    paths-ignore:
      - "**.md"
      - "**/CHANGELOG*"
      - "**/*.lock"

concurrency:
  group: codex-review-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  review:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      pull-requests: write
      issues: read
      id-token: write

    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 1
          ref: refs/pull/${{ github.event.pull_request.number }}/merge

      - name: Pre-fetch base + head refs
        env:
          PR_BASE_REF: ${{ github.event.pull_request.base.ref }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          git fetch --no-tags origin "$PR_BASE_REF" "+refs/pull/$PR_NUMBER/head"

      - name: Run Codex review
        id: run_codex
        uses: openai/codex-action@v1.8
        with:
          openai-api-key: ${{ secrets.OPENAI_API_KEY }}
          prompt: |
            Review PR ${{ github.repository }}/pull/${{ github.event.pull_request.number }}.

            This is the STATIC FALLBACK template (no project tailoring). Use
            scripts/render-workflows.sh for a project-specific prompt baked in
            at render time. Until then, apply general best practices:
            - Cite file:line for every issue.
            - Group findings by severity: Reject / Major / Minor / Nit.
            - Skip trivial nits; lead with structural concerns.
            - Be concise.

      - name: Post review as PR comment
        if: ${{ steps.run_codex.outputs.final-message != '' }}
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          BODY: ${{ steps.run_codex.outputs.final-message }}
          GH_TOKEN: ${{ github.token }}
        run: |
          gh pr comment "$PR_NUMBER" --body "$BODY"
