{
  "_comment": "Superset workspace config installed by 24hour-ClaudeCode (via /24hour-ClaudeCode:setup or scripts/install-superset-config.sh). References three scripts: setup.sh (verify+init+deps), run.sh (dev server), teardown.sh (cleanup). Edit those scripts to fit your project; this config just declares which file each Superset hook runs.",

  "setup": ["./.superset/setup.sh"],
  "teardown": ["./.superset/teardown.sh"],
  "run": ["./.superset/run.sh"]
}
