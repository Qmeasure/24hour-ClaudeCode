{
  "_comment": "Project-level config for 24hour-ClaudeCode plugin. Installed by /24hour-ClaudeCode:setup. Edit by hand or via the enable/disable commands. Schema: enabled (bool, master switch), github.merge_method, checks (local verify commands for the review-loop skill), repair (loop discipline), danger_paths (paths requiring explicit care).",

  "enabled": true,

  "github": {
    "merge_method": "squash",
    "require_external_ci": false
  },

  "checks": {
    "run_local_tests": true,
    "commands": []
  },

  "repair": {
    "enabled": true,
    "max_iterations": 5,
    "review_loop_timeout": 7200,
    "review_loop_interval": 20,
    "print_event_timeline": true,
    "stop_on_repeated_failure": true,
    "allow_workflow_in_pr": false
  },

  "danger_paths": [
    "migrations/**",
    "db/migrations/**",
    "alembic/versions/**",
    "prisma/migrations/**",
    "prisma/schema.prisma",
    "infra/**",
    "terraform/**",
    "k8s/**",
    "helm/**",
    ".env.production",
    ".env.staging",
    "**/secrets/**",
    "**/credentials/**"
  ]
}
